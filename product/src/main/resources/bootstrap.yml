spring:
  application:
    name: product
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
  jpa:
    show-sql: true
eureka:
  client:
    service-url:
      defauliZone: http://localhost:8761/eureka-server