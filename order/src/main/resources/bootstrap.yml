spring:
  application:
    name: order
  cloud:
      config:
        discovery:
          enabled: true
          service-id: CONFIG
        profile: test
  jpa:
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka-server
server:
  port: 8084